#include <cstdio>
#include <cassert>
#include <cstring>
#include <gmp.h>

int main() {
	int N, K, L, T;
	for (scanf("%d", &T); T-- > 0;) {
		scanf("%d %d %d", &N, &K, &L);

		mpf_t logf;
		mpf_init2(logf, 1024*2);

		mpf_clear(logf);	
	}
}
